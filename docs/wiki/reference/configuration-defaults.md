# Configuration Defaults

PostGWAS assembles its packaged configuration from application, pipeline,
resource, and module YAML files. User configuration and explicit command-line
overrides are layered on top; hardware-dependent `auto` values are resolved at
run time. Use `postgwas config show` to inspect the effective configuration for
your installed version and machine.

<!-- GENERATED: PACKAGED CONFIGURATION DEFAULTS -->
